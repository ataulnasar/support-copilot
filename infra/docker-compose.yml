services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: supportcopilot
      POSTGRES_USER: support
      POSTGRES_PASSWORD: support
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data

volumes:
  dbdata:
